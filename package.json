{
  "name": "schema-generator",
  "version": "1.0.0",
  "description": "Generate JSON schema from intermediary JSONIX JSON schema. JSONIX JSON schema is generated from XML Schema with JAXB/XJC.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "[George Moyano] (https://onename.com/gmoyano)",
  "license": "MIT",
  "dependencies": {
    "json-schema-ref-parser": "^3.1.2"
  }
}
